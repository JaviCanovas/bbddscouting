/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['your-project.supabase.co'], // replace with your Supabase project domain
  },
  reactStrictMode: true,
};

export default nextConfig;
